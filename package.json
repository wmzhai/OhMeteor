{
  "name": "meteor-intro",
  "version": "0.0.1",
  "description": "Meteor Introduction",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Weiming Zhai",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "http://github.com/wmzhai/meteor-intro.git"
  },
  "scripts": {
    "b": "mkdir -p out; cp resources/*.css out; spec-md content/index.md > out/index.html",
    "s": "cd out && python -m SimpleHTTPServer",
    "w": "onchange './content/*.md' -- npm run build",
    "d": "sh ./resources/deploy.sh",
    "c": "rm -rf out"
  }
}
